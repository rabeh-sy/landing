# Public HTTPS origin of the Badenjki Business Rails API. Astro dev also accepts
# http://localhost:3000. Add this website origin
# to the API's WEBSITE_ALLOWED_ORIGINS for CORS and Origin checks.
PUBLIC_BADENJKI_API_ORIGIN=http://localhost:3000
# Direct source and attribution window are configured only in Rails.
