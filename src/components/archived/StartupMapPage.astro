---
import Layout from '~/layouts/PageLayout.astro';
import startups from '~/data/startups.json';

const metadata = {
  title: 'خريطة الشركات الناشئة في سوريا',
  description: 'استكشف النظام البيئي للشركات الناشئة في سوريا',
};

// Archived for later reuse. Keeping the implementation outside src/pages
// removes the public route without losing the page code.
const categories = [...new Set(startups.map((s) => s.category))];
const categoryColors = {
  البرمجيات: '#10b981',
  'الطعام والتوصيل': '#3b82f6',
  'النقل الذكي': '#f59e0b',
  الصحة: '#ef4444',
  'التجارة الإلكترونية': '#8b5cf6',
  أخرى: '#6b7280',
};
---

<Layout metadata={metadata}>
  <section class="py-16 md:py-20">
    <div class="container mx-auto px-4 sm:px-6 max-w-7xl">
      <div class="text-center mb-12">
        <h1 class="text-4xl md:text-5xl font-bold leading-tight mb-4 text-gray-900 dark:text-white">
          خريطة الشركات الناشئة في سوريا
        </h1>
        <p class="text-xl text-gray-600 dark:text-gray-400 max-w-3xl mx-auto">
          استكشف النظام البيئي للشركات الناشئة السورية والابتكارات التقنية
        </p>
      </div>

      <div class="mb-8 flex flex-wrap justify-center gap-4">
        <button
          class="category-filter active flex items-center gap-2 px-4 py-2 bg-white dark:bg-slate-800 rounded-lg shadow-sm hover:shadow-md transition-all cursor-pointer border-2 border-transparent hover:border-gray-300"
          data-category="all"
        >
          <span class="text-sm font-medium text-gray-700 dark:text-gray-300">الكل</span>
        </button>
        {categories.map((category) => (
          <button
            class="category-filter flex items-center gap-2 px-4 py-2 bg-white dark:bg-slate-800 rounded-lg shadow-sm hover:shadow-md transition-all cursor-pointer border-2 border-transparent hover:border-gray-300"
            data-category={category}
          >
            <div
              class="w-4 h-4 rounded-full"
              style={`background-color: ${categoryColors[category] || categoryColors['أخرى']}`}
            />
            <span class="text-sm font-medium text-gray-700 dark:text-gray-300">{category}</span>
          </button>
        ))}
      </div>

      <div class="bg-gradient-to-br from-amber-50 to-green-50 dark:from-slate-900 dark:to-emerald-950 rounded-2xl p-8 md:p-12 shadow-xl min-h-[600px] relative overflow-hidden">
        <div class="absolute inset-0 opacity-5">
          <svg width="100%" height="100%">
            <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
              <circle cx="20" cy="20" r="1" fill="currentColor" />
            </pattern>
            <rect width="100%" height="100%" fill="url(#grid)" />
          </svg>
        </div>

        <div id="bubble-container" class="relative z-10 flex flex-wrap justify-center items-center gap-6 min-h-[500px]">
          {startups.map((startup, index) => {
            const sizeClasses = {
              small: 'w-32 h-32 text-sm',
              medium: 'w-40 h-40 text-base',
              large: 'w-48 h-48 text-lg',
            };
            const size = startup.size || 'medium';
            const color = categoryColors[startup.category] || categoryColors['أخرى'];

            return (
              <div
                class={`bubble-item ${sizeClasses[size]} flex flex-col items-center justify-center rounded-full cursor-pointer transition-all duration-300 hover:scale-110 hover:shadow-2xl group relative`}
                style={`background: linear-gradient(135deg, ${color}dd, ${color}aa); animation: float ${3 + index}s ease-in-out infinite;`}
                data-startup={JSON.stringify(startup)}
                data-category={startup.category}
              >
                <div class="mb-2 transform group-hover:scale-110 transition-transform">
                  <img
                    src={startup.logo}
                    alt={`${startup.name} logo`}
                    class="w-16 h-16 object-contain rounded-lg bg-white/90 p-2"
                    loading="lazy"
                  />
                </div>
                <div class="text-white font-bold text-center px-2">
                  {startup.name}
                </div>
                <div class="text-white/80 text-xs mt-1 text-center px-2">
                  {startup.category}
                </div>
              </div>
            );
          })}
        </div>
      </div>

      <div id="startup-modal" class="hidden fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4" style="direction: rtl;">
        <div class="bg-white dark:bg-slate-800 rounded-2xl max-w-2xl w-full p-8 shadow-2xl transform transition-all">
          <div class="flex justify-between items-start mb-6">
            <div class="flex items-center gap-4">
              <div class="w-20 h-20 bg-gray-100 dark:bg-gray-700 rounded-xl flex items-center justify-center p-2">
                <img id="modal-logo" src="" alt="Startup logo" class="w-full h-full object-contain" />
              </div>
              <div>
                <h3 id="modal-title" class="text-2xl font-bold text-gray-900 dark:text-white"></h3>
                <p id="modal-category" class="text-gray-600 dark:text-gray-400"></p>
              </div>
            </div>
            <button
              id="close-modal"
              class="text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 text-2xl leading-none"
            >
              ×
            </button>
          </div>

          <p id="modal-description" class="text-gray-700 dark:text-gray-300 mb-6 text-lg"></p>

          <div id="modal-founders-section" class="mb-6">
            <h4 class="text-sm font-semibold text-gray-600 dark:text-gray-400 mb-3">المؤسسون:</h4>
            <div id="modal-founders" class="flex flex-wrap gap-3"></div>
          </div>

          <a
            id="modal-link"
            href="#"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 bg-gradient-to-r from-emerald-600 to-green-700 hover:from-emerald-700 hover:to-green-800 text-white px-6 py-3 rounded-lg font-medium transition-all hover:shadow-lg"
          >
            زيارة الموقع
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
            </svg>
          </a>
        </div>
      </div>

      <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="bg-white dark:bg-slate-800 rounded-xl p-6 text-center shadow-lg">
          <div class="text-4xl font-bold text-emerald-600 dark:text-emerald-400 mb-2">
            {startups.length}
          </div>
          <div class="text-gray-600 dark:text-gray-400 font-medium">
            شركة ناشئة
          </div>
        </div>

        <div class="bg-white dark:bg-slate-800 rounded-xl p-6 text-center shadow-lg">
          <div class="text-4xl font-bold text-green-600 dark:text-green-400 mb-2">
            {categories.length}
          </div>
          <div class="text-gray-600 dark:text-gray-400 font-medium">
            قطاع
          </div>
        </div>

        <div class="bg-white dark:bg-slate-800 rounded-xl p-6 text-center shadow-lg">
          <div class="text-4xl font-bold text-amber-600 dark:text-amber-400 mb-2">
            🚀
          </div>
          <div class="text-gray-600 dark:text-gray-400 font-medium">
            نظام بيئي متنامي
          </div>
        </div>
      </div>

      <div class="mt-12 bg-gradient-to-r from-emerald-600 to-green-700 dark:from-emerald-700 dark:to-green-800 rounded-2xl p-8 text-center shadow-xl">
        <h3 class="text-2xl md:text-3xl font-bold text-white mb-4">
          هل لديك شركة ناشئة سورية؟
        </h3>
        <p class="text-emerald-100 text-lg mb-6 max-w-2xl mx-auto">
          نود إضافة شركتك إلى الخريطة. تواصل معنا لنضيفها ونساهم في دعم النظام البيئي للابتكار في سوريا
        </p>
        <a
          href="/contact"
          class="inline-block bg-white text-emerald-700 px-8 py-3 rounded-lg font-bold hover:bg-emerald-50 transition-colors shadow-lg hover:shadow-xl"
        >
          اتصل بنا
        </a>
      </div>
    </div>
  </section>

  <style>
    @keyframes float {
      0%, 100% {
        transform: translateY(0px);
      }
      50% {
        transform: translateY(-20px);
      }
    }

    .bubble-item {
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    }

    .bubble-item:hover {
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    }

    .bubble-item.hidden-bubble {
      display: none;
    }

    .category-filter.active {
      border-color: rgb(16 185 129) !important;
      background-color: rgb(16 185 129 / 0.1);
    }

    .category-filter.active span {
      font-weight: 700;
      color: rgb(16 185 129);
    }

    .dark .category-filter.active span {
      color: rgb(52 211 153);
    }

    #startup-modal {
      backdrop-filter: blur(8px);
    }
  </style>

  <script>
    function initStartupMap() {
      const modal = document.getElementById('startup-modal');
      const closeModal = document.getElementById('close-modal');
      const bubbles = document.querySelectorAll('.bubble-item');
      const categoryFilters = document.querySelectorAll('.category-filter');

      if (!modal || !closeModal || bubbles.length === 0) {
        return;
      }

      categoryFilters.forEach((filter) => {
        filter.addEventListener('click', () => {
          const selectedCategory = filter.getAttribute('data-category');

          categoryFilters.forEach((f) => f.classList.remove('active'));
          filter.classList.add('active');

          bubbles.forEach((bubble) => {
            const bubbleCategory = bubble.getAttribute('data-category');
            if (selectedCategory === 'all' || bubbleCategory === selectedCategory) {
              bubble.classList.remove('hidden-bubble');
            } else {
              bubble.classList.add('hidden-bubble');
            }
          });
        });
      });

      const modalLogo = document.getElementById('modal-logo');
      const modalTitle = document.getElementById('modal-title');
      const modalCategory = document.getElementById('modal-category');
      const modalDescription = document.getElementById('modal-description');
      const modalLink = document.getElementById('modal-link');
      const modalFounders = document.getElementById('modal-founders');
      const modalFoundersSection = document.getElementById('modal-founders-section');

      const openModal = () => {
        modal.classList.remove('hidden');
        document.body.style.overflow = 'hidden';
      };

      const hideModal = () => {
        modal.classList.add('hidden');
        document.body.style.overflow = '';
      };

      closeModal.addEventListener('click', hideModal);

      modal.addEventListener('click', (event) => {
        if (event.target === modal) {
          hideModal();
        }
      });

      document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape' && !modal.classList.contains('hidden')) {
          hideModal();
        }
      });

      bubbles.forEach((bubble) => {
        bubble.addEventListener('click', () => {
          const startupData = bubble.getAttribute('data-startup');
          if (!startupData) return;

          const startup = JSON.parse(startupData);

          if (modalLogo instanceof HTMLImageElement) {
            modalLogo.src = startup.logo;
            modalLogo.alt = startup.name + ' logo';
          }
          if (modalTitle) modalTitle.textContent = startup.name;
          if (modalCategory) modalCategory.textContent = startup.category;
          if (modalDescription) modalDescription.textContent = startup.description;
          if (modalLink) modalLink.setAttribute('href', startup.link);

          if (modalFounders && modalFoundersSection) {
            modalFounders.innerHTML = '';

            if (startup.founders && startup.founders.length > 0) {
              modalFoundersSection.classList.remove('hidden');

              startup.founders.forEach((founder: { name: string; linkedin: string }) => {
                const founderLink = document.createElement('a');
                founderLink.href = founder.linkedin;
                founderLink.target = '_blank';
                founderLink.rel = 'noopener noreferrer';
                founderLink.className =
                  'inline-flex items-center gap-2 px-4 py-2 bg-gray-100 hover:bg-gray-200 dark:bg-slate-700 dark:hover:bg-slate-600 rounded-lg transition-colors';
                founderLink.innerHTML = `
                  <span class="font-medium text-gray-800 dark:text-gray-200">${founder.name}</span>
                  <svg class="w-4 h-4 text-blue-600" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.762 2.239 5 5 5h14c2.762 0 5-2.238 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/>
                  </svg>
                `;
                modalFounders.appendChild(founderLink);
              });
            } else {
              modalFoundersSection.classList.add('hidden');
            }
          }

          openModal();
        });
      });
    }

    document.addEventListener('astro:page-load', initStartupMap);
  </script>
</Layout>
